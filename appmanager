#!/usr/bin/env php

<?php

class Appmanager{

    private string $app_json;
    private string $app_log;
    private string $path;
    private string $ok = "[✔]";
    private string $error = "[✖]";
    private string $info = "[ℹ]";

    public function __construct()
    {

        $this->path = getenv("HOME");
        $this->app_json = $this->path . "/appmanager/app_command.json";
        $this->app_log = $this->path . "/appmanager/log";
        
    }

    public function run(string $command, string $app_text) : void{

        //============================================
        // Creating folder and json file
        //============================================

        if(!is_dir($this->path . "/appmanager")) mkdir($this->path . "/appmanager", 0755, true);

        if(!is_dir($this->app_log)) mkdir($this->app_log, 0755, true);

        if(!file_exists($this->app_json)) file_put_contents($this->app_json, "[]", LOCK_EX);

        match($command){

            "home" => $this->Home_command(),

            "add" => $this->Add_command(),

            "reset" => $this->Reset_command(),

            "delete" => $this->Delete_command(),

            "list" => $this->List_command(),

            "run" => $this->Run_command($app_text),

            "log" => $this->Clear_logs($app_text),

            default => (function(){

                echo "\n";
                echo "Command not found\n";
                echo "Use : appmanager or php appmanager\n";
                echo "Use : appmanager list or php appmanager list\n";
                echo "Use : appmanager add or php appmanager add\n";
                echo "Use : appmanager delete or php appmanager delete\n";
                echo "Use : appmanager reset or php appmanager reset\n";
                echo "Use : appmanager run [id] or php appmanager run [id]\n";
                echo "Use : appmanager log clear or php appmanager log clear\n";

            })()

        };

    }

    private function Home_command(): void{

        echo "App:\n\n";

        //===================================
        // All app list
        //===================================

        $app_list = json_decode(file_get_contents($this->app_json), true) ?? []; 

        if(empty($app_list)){

            $this->save_log($this->info, "No Apps Found");

            echo $this->info . " No Apps Found.\n\n";

            echo "> First add an app:\n";
            echo "   appmanager add or php appmanager add\n\n";

            echo "> Available commands:\n";
            echo "   appmanager list or php appmanager list\n";
            echo "   appmanager add or php appmanager add\n";
            echo "   appmanager delete or php appmanager delete\n";
            echo "   appmanager reset or php appmanager reset\n";
            echo "   appmanager run <id> or php appmanager run <id>\n";
            echo "   appmanager log clear or php appmanager log clear\n";

            exit;

        }

        foreach ($app_list as $app){

            $this->output($app["app_id"], $app["app_title"]);

        }

        echo "\n";
        echo "Enter App ID:\n";

        $user_app_id = readline("=>");      // user input

        $s_user_app_id = trim(preg_replace("/[^\p{N}]/", "", $user_app_id ?? ""));    //sanitize user input

        if(empty($s_user_app_id)){

            $this->save_log($this->error, "Invalid ID");

            echo $this->error . " Invalid ID\n";
            exit;

        }

        //search number
        $isFound = false;
        $app_title = "";
        $app_command = [];

        foreach ($app_list as $list){

            if(($list["app_id"] ?? null) == $s_user_app_id){

                $isFound = true;
                $app_title = $list["app_title"];
                $app_command = $list["app_command"];
                break;

            }

        }

        if(!$isFound){

            $this->save_log($this->error, "Not Found");

            $this->app_print($this->error . " Not Found");

        }

        echo "\033[H\033[J";

        echo "================================================\n";
        echo"Launching $app_title\n";
        echo "================================================\n";
        
        foreach ($app_command as $command){

            if(!empty($command)){

                $this->save_log("COMMAND", "[$command] is executed");

                passthru(escapeshellcmd($command) . " 2>&1");

            }

        }

    }//fun end

    private function Add_command(): void{

        //initil items
        $id = "";
        $title = "";
        $first_command = "";
        $second_command = "";
        $third_command = "";
        $fourth_command = "";
        $fifth_command = "";

        $is_single_command = false;
        $is_multiple_command = false;

        echo "Select Command:\n";
        $this->output("1", "Single Command");
        $this->output("2", "Multiple Command");

        //========================
        //User input
        //========================

        echo "\n";
        $selected_id = readline("=>");
        echo "\n";

        if($selected_id === "1"){

            $is_single_command = true;

            echo "Enter App ID :\n";
            $id = readline("=>");
            echo "\n";

            echo "Enter App Title :\n";
            $title = readline("=>");
            echo "\n";

            echo "Enter App Command :\n";
            $first_command = readline("=>");


        }elseif($selected_id === "2"){

            $is_multiple_command = true;

            echo "Enter App ID :\n";
            $id = readline("=>");
            echo "\n";

            echo "Enter App Title :\n";
            $title = readline("=>");
            echo "\n";

            echo "Enter 1st Command :\n";
            $first_command = readline("=>");
            echo "\n";

            echo "Enter 2nd Command :\n";
            $second_command = readline("=>");
            echo "\n";

            echo "Enter 3rd Command :\n";
            $third_command = readline("=>");
            echo "\n";

            echo "Enter 4th Command :\n";
            $fourth_command = readline("=>");
            echo "\n";

            echo "Enter 5th Command :\n";
            $fifth_command = readline("=>");

        }else{

            $this->save_log($this->error, "Invalid ID");
            $this->app_print($this->error . " Invalid ID");

        }

        //=============================
        //Sanitizing data
        //=============================

        $s_id = trim(preg_replace("/[^\p{N}]/", "", $id ?? ""));
        $s_title = trim($title);
        $s1st_command = trim($first_command);
        $s2nd_command = trim($second_command);
        $s3th_command = trim($third_command);
        $s4th_command = trim($fourth_command);
        $s5th_command = trim($fifth_command);

        //===========================
        //Empty data checks
        //===========================

        if($is_single_command){

            if(empty($s_id) || empty($s_title) || empty($s1st_command)){

                $this->save_log($this->error, "[$id] [$title] [$first_command] Invalid ID / Text / Command");

                $this->app_print($this->error . " Invalid ID / Text / Command");

            }

        }else{

            if((empty($s_id) || empty($s_title)) || (empty($s1st_command) && 
            empty($s2nd_command) && empty($s3th_command) && empty($s4th_command) && empty($s5th_command))){

                $this->save_log($this->error, "[$id] [$title] [$first_command] [$second_command] [$third_command] [$fourth_command] [$fifth_command] Invalid ID / Text / Command");

                $this->app_print($this->error . " Invalid ID / Text / Command");

            }

        }

        //================================
        //Check app in list
        //================================

        $app_list = json_decode(file_get_contents($this->app_json), true) ?? [];

        if(!empty($app_list)){

            foreach ($app_list as $app){

                if(($app["app_id"] ?? null) == $s_id){

                    $this->save_log($this->error, "[$s_id] Duplicate App Found");

                    $this->app_print($this->error . " Duplicate App Found");

                }

            }

        }

        //===============================
        //Insert user app in list
        //===============================

        if($is_single_command){

            $data = [

                "app_id" => $s_id,
                "app_title" => $s_title,
                "app_command" => [ $s1st_command ]

            ];

        }else{

            $data = [

                "app_id" => $s_id,
                "app_title" => $s_title,
                "app_command" => [ 

                    $s1st_command ?? "",
                    $s2nd_command ?? "",
                    $s3th_command ?? "",
                    $s4th_command ?? "",
                    $s5th_command ?? ""

                 ]

            ];

        }

        $app_list[] = $data;

        $insert = file_put_contents($this->app_json, json_encode($app_list, JSON_PRETTY_PRINT | 
        JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE), LOCK_EX);

        chmod($this->app_json, 0600);
            
        if(!$insert){

            $this->save_log($this->error, "App Not Saved");

            $this->app_print($this->error . " App Not Saved");

        }

        $this->save_log($this->ok, "[$title] App Saved Successfully");

        $this->app_print($this->ok . " App Saved Successfully");
            
    }//fun end

    private function Delete_command(): void{

        echo "Enter App ID :\n";

        $id = readline("=>"); //user input

        $s_id = trim(preg_replace("/[^\p{N}]/", "", $id ?? ""));  //sanitize id

        if(empty($s_id)){

            $this->save_log($this->error, "[$id] Invalid ID");

            $this->app_print($this->error . " Invalid ID");

        }

        //================================
        //Check app in list
        //================================

        $app_list = json_decode(file_get_contents($this->app_json), true) ?? [];
        $isDeleted = false;

        if(!empty($app_list)){

            foreach ($app_list as $index => $app){

                if(($app["id"] ?? null) == $s_id){

                    unset($app_list[$index]);
                    $isDeleted = true;
                    break;

                }

            }

        }

        if($isDeleted){

            $app_list = array_values($app_list);

            file_put_contents($this->app_json, json_encode($app_list, JSON_PRETTY_PRINT | 
                JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE), LOCK_EX);

            $this->save_log($this->ok, "Delete Successfully");

            $this->app_print($this->ok . " Delete Successfully");

        }

        $this->save_log($this->error, "Delete Failed");

        $this->app_print($this->error . " Delete Failed");
        
    }//fun end

    private function Reset_command(): void{

        $isUserConfirmed = false;

        $app_list = json_decode(file_get_contents($this->app_json), true) ?? [];

        if(empty($app_list)){

            $this->save_log($this->info, "Empty App List");

            echo $this->info . " Empty App List\n\n";

            echo "> Add your first app:\n";
            echo "   appmanager or php appmanager add\n\n";
            exit;

        }

        echo "Are you sure you want reset all [Y/n]:\n";

        $input = readline("=>");    //user input

        $s_input = trim(preg_replace("/[^a-zA-Z]/", "", $input ?? ""));     //sanitize user input

        if(empty($s_input)){

            $this->save_log($this->error, "[$input] Invalid Text");

            $this->app_print($this->error . " Invalid Text");

        }

        if(strtolower($s_input) === "y"){

            $isUserConfirmed = true;

        }else{

            $isUserConfirmed = false;

        }

        if($isUserConfirmed){

            file_put_contents($this->app_json, "[]", LOCK_EX);

            $this->save_log($this->ok, "Reset Successfully");

            $this->app_print($this->ok . " Reset Successfully");

        }

        $this->save_log($this->error, "Reset Failed");
        $this->app_print($this->error . " Reset Failed");
            
    }//fun end

    private function List_command(): void{

        $green_color = "\033[32m";
        $bold_text = "\e[1m";
        $reset = "\e[0m";

        //===============================
        // App list
        //===============================
            
        $app_list = json_decode(file_get_contents($this->app_json), true) ?? [];

        if(!empty($app_list)){

            echo " No  >> Name    >> Command\n\n";

            //==============================
            // Get all app list
            //==============================

            foreach ($app_list as $app_data){

                $id = $app_data["app_id"];
                $title = $app_data["app_title"];
                $command = $app_data["app_command"];

                $command = "[ " . implode(" ], [ ", $command) . " ]";

                printf(" %-3s >> %-15s >> %-70s \n", $id, $bold_text . $title . $reset, $green_color . $bold_text . $command . $reset);

            }


        }else{

            $this->save_log($this->info, "Empty App List");

            echo $this->info . " Empty App List\n\n";

            echo "> Add your first app:\n";
            echo "   appmanager add or php appmanager add\n\n";

        }

    }//fun end

    private function Run_command(string $app_id) : void{

        //=================================
        // Sanitizing the id
        //=================================
        $s_app_id = preg_replace("/[^\p{N}]/", "", $app_id);

        $app_list = json_decode(file_get_contents($this->app_json), true) ?? [];

        if(empty($s_app_id)){

            $this->save_log($this->error, "[$app_id] Invalid ID");
            $this->app_print($this->error . " Invalid ID");

        }

        if(!empty($app_list)){

            $isFound = false;
            $app_id = "";
            $app_command = [];

            //==============================
            // Search app id in list
            //==============================

            foreach ($app_list as $list){

                if(($list["app_id"] ?? null) == $s_app_id){

                    $isFound = true;
                    $app_id = $list["app_id"];
                    $app_command = $list["app_command"];
                    break;

                }

            }

            if(!$isFound){

                $this->save_log($this->error, "[$s_app_id] Not Found in List");
                $this->app_print($this->error . " App Not Found");

            }

            foreach ($app_command as $command){

                $this->save_log("COMMAND", "[$command] is executed");

                passthru(escapeshellcmd($command) . " 2>&1");

            }


        }else{

            $this->save_log($this->error, "App List Not Found");
            $this->app_print($this->error . " Not Found");

        }

    }//fun end

    private function Clear_logs(string $text) : void{

        if($text !== "clear"){

            $this->save_log("INFO", "Use : app log clear or php app log clear");
            $this->app_print("[INFO] Use : app log clear or php app log clear");

        }

        echo "Are you sure? [Y/n]:\n";
        $user_confirmation = readline("=>");

        if(strtolower($user_confirmation) === "y"){

            $files = glob($this->app_log . "/*.txt");

            foreach ($files as $file){

                unlink($file);

            }

            $this->save_log($this->ok, "Logs Deleted Successfully");
            $this->app_print($this->ok . " Logs Deleted Successfully");

        }else{

            $this->save_log($this->error, "Logs Delete Failed");
            $this->app_print($this->error . " Logs Delete Failed");

        }


    }//fun end

    private function output(string $number, string $text) : void{

        $green_color = "\033[32m";
        $bold_text = "\e[1m";
        $reset = "\e[0m";

        echo "[" . $green_color . $bold_text . $number . $reset . "]" . " " . $green_color . $bold_text . $text . $reset . "\n";

    }//fun end

    private function app_print(string $text) : void{

        echo "\n";
        echo "$text\n";
        exit;

    }//fun end

    private function save_log(string $type, string $message){

        date_default_timezone_set("UTC");

        $date = date("Y-m-d h:i:s A");

        //=============================
        // Monthly log file
        //=============================

        $month = date("Y-m");

        $log = "[$date] >> [$type] >> $message";

        file_put_contents($this->app_log . "/{$month}_log.txt", $log . PHP_EOL, FILE_APPEND);

    }//fun end


}//class

$command = $argv[1] ?? "home";
$app_text = $argv[2] ?? "";
$appmanager = new Appmanager();
$appmanager->run($command, $app_text);

